<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.unfoldingword.mobile" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:screenOrientation="portrait"
            android:name="activity.SplashScreenActivity"
            android:configChanges="orientation"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="activity.bookSelection.StoryChapterSelectionActivity"
            android:configChanges="orientation|keyboardHidden"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name="activity.bookSelection.InitialScreenActivity"
            android:configChanges="orientation|keyboardHidden"
            android:theme="@style/MainTheme" />
        <activity
            android:name="activity.bookSelection.BookSelectionActivity"
            android:configChanges="orientation|keyboardHidden"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name="activity.bookSelection.BibleChapterSelectionActivity"
            android:configChanges="orientation|keyboardHidden"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name="activity.bookSelection.VersionSelectionActivity"
            android:configChanges="orientation|keyboardHidden"
            android:theme="@style/MainTheme" />
        <activity
            android:name="activity.reading.ReadingActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|uiMode"
            android:theme="@style/MainTheme"/>

        <activity
            android:name="activity.reading.StoryReadingActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|uiMode"
            android:theme="@style/MainTheme" />

        <activity
            android:name="activity.SettingsActivity"
            android:label="@string/title_activity_settings" >
        </activity>

        <service android:name="services.UpdateService" />
        <service android:name="services.UWUpdater" />
        <service android:name="services.VersionDownloadService" />
        <service android:name="services.UWVersionDownloader" />
        <service android:name="services.UWPreLoader" />

    </application>

</manifest>
