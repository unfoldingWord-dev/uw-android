<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.unfoldingword.mobile" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:screenOrientation="portrait"
            android:name="activity.SplashScreenActivity"
            android:configChanges="orientation"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="activity.textSelection.StoryChapterSelectionActivity"
            android:configChanges="orientation|keyboardHidden"
            android:theme="@style/MainTheme" />
        <activity
            android:name="activity.textSelection.InitialScreenActivity"
            android:configChanges="orientation|keyboardHidden"
            android:theme="@style/MainTheme" />
        <activity
            android:name="activity.textSelection.BookSelectionActivity"
            android:configChanges="orientation|keyboardHidden"
            android:theme="@style/MainTheme" />
        <activity
            android:name="activity.textSelection.BibleChapterSelectionActivity"
            android:configChanges="orientation|keyboardHidden"
            android:theme="@style/MainTheme" />
        <activity
            android:name="activity.textSelection.VersionSelectionActivity"
            android:configChanges="orientation|keyboardHidden"
            android:theme="@style/MainTheme" />
        <activity
            android:name="activity.reading.ReadingActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|uiMode"
            android:theme="@style/MainTheme"/>

        <activity
            android:name="activity.reading.StoryReadingActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|uiMode"
            android:theme="@style/MainTheme" />

        <activity
            android:name="activity.sharing.LoadActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|uiMode"
            android:theme="@style/MainTheme">

            <intent-filter android:label="@string/app_name" >
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:mimeType="application/octet-stream"
                    android:pathPattern=".*\\.ufw"
                    android:scheme="content" />
            </intent-filter>
            <!--<intent-filter android:label="unfoldingWord" >-->
                <!--<action android:name="android.intent.action.VIEW" />-->

                <!--<category android:name="android.intent.category.DEFAULT" />-->

                <!--<data-->
                    <!--android:mimeType="*/*"-->
                    <!--android:pathPattern=".*\\.ufw"-->
                    <!--android:scheme="file" />-->
            <!--</intent-filter>-->
            <intent-filter android:label="@string/app_name" >
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:scheme="file" />
                <data android:host="*" />
                <data android:pathPattern=".*\\.ufw" />
            </intent-filter>
            <intent-filter android:label="@string/app_name" >
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:host="*"
                    android:mimeType="*/*"
                    android:pathPattern=".*\\.ufw"
                    android:scheme="file" />
            </intent-filter>
            </activity>

        <activity
            android:name="activity.sharing.ShareActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|uiMode"
            android:theme="@style/MainTheme" />

        <activity
            android:name="activity.sharing.FileFinderActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|uiMode"
            android:theme="@style/MainTheme" />

        <activity
            android:name="activity.sharing.BluetoothReceivingActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|uiMode"
            android:theme="@style/MainTheme" />

        <activity
            android:name="activity.sharing.BluetoothSharingActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|uiMode"
            android:theme="@style/MainTheme" />

        <activity
            android:name="wifiDirect.WiFiDirectActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|uiMode"
            android:theme="@style/MainTheme" />

        <activity
            android:name="activity.SettingsActivity"
            android:label="@string/title_activity_settings" >
        </activity>

        <service android:name="services.UWUpdaterService" />
        <service android:name="services.UWVersionDownloaderService" />
        <service android:name="services.UWPreLoaderService" />
        <service android:name="services.UWSideLoaderService" />

    </application>

</manifest>
